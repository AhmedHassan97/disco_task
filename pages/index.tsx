import type { NextPage } from "next";
import LoginComponent from "../app/components/login/login";
const Home: NextPage = () => {
  return (
    <div>
      <LoginComponent />
    </div>
  );
};

export default Home;
